import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    container: {

    },
    image: {
        height: 80,
        width: 80,
        borderRadius: 40
    }
})

export default styles;