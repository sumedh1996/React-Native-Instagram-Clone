import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    container: {
        margin: 10,
        borderRadius: 43,
        borderWidth: 3,
        borderColor: '#AE22E0'
    },
    image: {
        borderRadius: 40,
        borderWidth: 1,
        borderColor: '#FFFFFF'
        
    }
})

export default styles;